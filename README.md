# sample-csv-parser
Takes the provided CSV and outputs structured JSON

To run, in terminal run the following command from the cloned repo root:

```bash
cat examples.csv | node ./parser.js
```

To run the PHP version, run the following command from the cloned repo root:

```bash
php parser.php
```
